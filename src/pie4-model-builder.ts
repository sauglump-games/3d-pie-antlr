import type { PIEParseResult } from "./pie-model";

/** Lex, parse and build a PIE4 document into header + levels. */
export function parsePIE4(_data: string): PIEParseResult {
  throw new Error("parsePIE4 not implemented yet.");
}
