import type { PIEParseResult } from "./pie-model";

/** Lex, parse and build a PIE2 document into header + levels. */
export function parsePIE2(_data: string): PIEParseResult {
  throw new Error("parsePIE2 not implemented yet.");
}
